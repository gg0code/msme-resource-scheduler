AUTO-GENERATED by generate_docs.py on 2026-04-01 16:37
Branch: v4-dev
────────────────────────────────────────────────────────────

FOLDER: frontend/src/api
BRANCH: v4-dev
PURPOSE: Contains TypeScript API client functions that provide the frontend interface to all backend REST endpoints.

FILES:
  api_dashboard.ts - Dashboard metrics and analytics API client functions, v4-dev
  api_employees.ts - Employee CRUD operations and management API client, v4-dev  
  api_jobs.ts - Job lifecycle management and operations API client, v4-dev
  api_machines.ts - Machine resource management and status API client, v4-dev
  api_resource_availability.ts - Resource availability checking and validation API client, v4-dev
  api_skills.ts - Skills management and assignment API client, v4-dev
  generate_docs.py - Automated documentation generator for API client files, v4-dev
  scheduling.ts - Scheduling engine operations and job assignment API client, v4-dev

ARCHITECTURE NOTES:
This folder serves as the centralized API client layer that bridges the React frontend with the FastAPI backend. Each file corresponds to a specific domain or router in the backend and uses the shared Axios client from client.ts for authentication and request handling. All API functions follow a consistent pattern of accepting typed parameters, making HTTP requests to /api/ prefixed endpoints, and returning properly typed responses. The functions are consumed by React components and hooks throughout the frontend to perform all data operations. This separation ensures clean architecture where UI components don't directly handle HTTP logic.

DESIGN PRINCIPLES:
Principle 4 applies as all API calls target /api/ prefixed backend routes. Principle 11 is enforced through TypeScript strict mode with proper typing for all request/response shapes using types from types_index.ts. Principle 2 is supported by ensuring tenant scoping is maintained through the authenticated Axios client that includes proper headers for backend tenant filtering.

GOTCHAS:
All API functions must use the shared Axios client from client.ts to ensure proper JWT token handling and automatic refresh logic works correctly. When adding new API functions, you must import and use the exact type definitions from types_index.ts rather than defining inline types to maintain consistency. Error handling should be consistent across all API functions and follow the established pattern of letting TanStack Query handle HTTP errors rather than catching them in the API layer.

DEPENDENCIES:
This folder depends on frontend/src/api/client.ts for the authenticated Axios instance, frontend/src/types/types_index.ts for all TypeScript type definitions, and indirectly on backend route handlers in backend/app/routers/ for the actual API endpoints. This folder is consumed by React components in frontend/src/pages/ and frontend/src/components/, as well as custom hooks and context providers throughout the frontend that need to interact with backend data.