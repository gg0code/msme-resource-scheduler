DATABASE_URL=postgresql://msme_user:msme_pass@localhost:5432/msme_scheduler
DB_HOST=localhost
DB_PORT=5432
DB_USER=msme_user
DB_PASSWORD=msme_pass
DB_NAME=msme_scheduler
BACKEND_PORT=8000
FRONTEND_PORT=3000
SECRET_KEY=98d2b9feaec9f01f666f61a472ee41ae08117d15912e8ee35230e2783aaccf01
ALLOWED_ORIGINS=http://localhost:8000
# ── v5-whatsapp settings ──────────────────────────────────────────────────
# Redis — get from upstash.com (free tier is enough for pilot)
# Leave blank for now — whatsapp_session.py uses mock mode if empty
UPSTASH_REDIS_URL=

# WhatsApp security tokens — fill in when you register Meta developer account (v5.2)
WHATSAPP_VERIFY_TOKEN=
WHATSAPP_APP_SECRET=

# Interakt — leave blank until v5.5
INTERAKT_API_KEY=

# Development mode — True means no real WhatsApp calls, logs to console instead
WHATSAPP_MOCK_MODE=True

# Whisper — local model for now, switch to api at v5.5
WHISPER_MODE=local
OPENAI_API_KEY=
