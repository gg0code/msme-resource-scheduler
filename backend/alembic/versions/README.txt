AUTO-GENERATED by generate_docs.py on 2026-04-01 16:52
Branch: v4-dev
────────────────────────────────────────────────────────────

FOLDER: backend/alembic/versions
BRANCH: both
PURPOSE: Contains Alembic database migration files that define the complete schema evolution history for the ZetaOps Copilot PostgreSQL database.

FILES:
  46385051aa8d_initial_schema.py - Initial database schema migration creating foundational tables, v4-dev
  002_add_cost_and_timer_fields.py - Adds cost tracking and timer functionality fields to existing tables, v4-dev
  003_add_tenant_id_stub.py - Adds nullable tenant_id column to all core tables for multitenancy preparation, v4-dev
  004_jwt_auth_multitenancy.py - Establishes JWT authentication foundation and enforces tenant isolation, v4-dev
  005_add_plan_to_tenants.py - Adds plan column to tenants table for subscription tiers, v4-dev
  006_add_hourly_rate_fields.py - Adds hourly rate and cost tracking columns to employees and machines, v4-dev
  007_add_ai_usage_tracking.py - Adds AI query usage tracking fields to tenants table for plan limits, v4-dev
  008_add_job_scheduling_fields.py - Adds advanced scheduling fields to support the core scheduling engine, v4-dev
  009_add_allocation_pct_to_assignments.py - Adds partial resource allocation percentage to job assignments, v4-dev
  010_add_generic_role_flag.py - Adds is_generic_role boolean flag to employee roles, v4-dev
  011_fix_skills_unique_constraint.py - Fixes tenant isolation issue in skills table constraints, v4-dev
  012_j1_2_job_fields.py - Adds customer relationship management fields to jobs table, v4-dev
  013_add_scheduling_tables.py - Creates foundational tables for step-based scheduling engine, v4-dev
  014_add_schedule_entries.py - Creates schedule entries table for tracking job execution timeline, v4-dev
  015_add_job_type_quantity.py - Adds job_type and quantity columns to jobs table, v4-dev
  016_add_industry_type_to_tenants.py - Adds industry_type column to tenants for industry-specific features, v4-dev
  017_phone_tenant_map.py - Creates WhatsApp phone number mapping and conversation tables, v5-whatsapp
  018_phone_tenant_map_roles.py - Adds role-based access control to WhatsApp phone mappings, v5-whatsapp

ARCHITECTURE NOTES:
These migration files form a linear chain from initial schema through migration 018, with each building upon the previous state. The migrations follow a clear evolution from basic job scheduling in early files to advanced multitenancy with tenant isolation in the middle migrations to WhatsApp Copilot features in the latest ones. Migration 003 and 004 represent a critical architectural shift where tenant_id was added to all tables and then made mandatory for proper data isolation. The v5-whatsapp branch includes all v4-dev migrations plus the WhatsApp-specific migrations 017 and 018. Each migration is reversible and includes both upgrade and downgrade operations for safe database state management.

DESIGN PRINCIPLES:
Principle 2 (tenant scoping) is foundational here as migrations 003-004 establish tenant_id on all tables and subsequent migrations maintain this pattern. Principle 8 (feature flags) is supported by migrations that add columns for feature gating like industry_type and AI usage tracking. The linear migration chain ensures database state consistency and supports the principle that all data access must be tenant-scoped for security.

GOTCHAS:
Never edit existing migration files after they have been applied to production as this will cause database state inconsistencies and break the migration chain. Always run migrations in sequence and never skip versions as later migrations depend on schema changes from earlier ones. The tenant_id field addition in migrations 003-004 is particularly sensitive and any rollback past migration 004 in production would cause catastrophic data loss due to tenant isolation requirements.

DEPENDENCIES:
This folder depends on the Alembic configuration in backend/alembic/alembic.ini and backend/alembic/env.py for migration execution context. The migrations reference SQLAlchemy table definitions that correspond to the models in backend/app/models/ but use raw SQL DDL rather than importing the model classes directly. The main application in backend/app/main.py depends on this folder indirectly as the database schema defined by these migrations must match the SQLAlchemy models used by the ORM throughout the application.