AUTO-GENERATED by generate_docs.py on 2026-04-01 18:08
Branch: v4-dev
────────────────────────────────────────────────────────────

FOLDER: backend/scripts
BRANCH: v4-dev
PURPOSE: Contains standalone database seeding and user management utilities for development and testing.

FILES:
  create_user.py - Command-line utility for creating new users in the system - v4-dev
  reset_and_seed_printing.py - Interactive database seeding script for printing industry vertical - v4-dev
  seed_data.py - General database seeding utility for populating test data - v4-dev
  seed_generic_roles.py - Creates generic workforce roles for manufacturing businesses - v4-dev
  seed_scheduling.py - Development utility for creating test scheduling data - v4-dev
  seed_v2.py - Standalone script for populating PostgreSQL with realistic test data - v4-dev

ARCHITECTURE NOTES:
These scripts are standalone utilities that run independently of the main FastAPI application to bootstrap development databases and create test scenarios. Each script connects directly to the database using SQLAlchemy sessions and creates realistic data for different aspects of the system (users, roles, scheduling, industry-specific data). The scripts are designed to be run manually during development or testing phases to quickly populate empty databases with meaningful data. They complement each other by covering different data domains - user management, industry verticals, scheduling scenarios, and workforce roles.

DESIGN PRINCIPLES:
Principle 2 applies - all scripts must respect tenant scoping when creating data and include tenant_id in all records. Principle 3 applies - scripts use settings configuration rather than hardcoded database credentials. These utilities support the overall system architecture by ensuring test data follows the same multi-tenant patterns as production data.

GOTCHAS:
Scripts directly manipulate the database and can destroy existing data, especially reset_and_seed_printing.py which clears tables before seeding. All scripts assume a properly configured database connection and will fail silently or with cryptic errors if database settings are incorrect. Scripts create users with default passwords that must be changed in production environments, and some may not validate tenant boundaries properly if run carelessly.

DEPENDENCIES:
This folder depends on backend/app/models/ for ORM definitions, backend/app/core/database.py for database connections, and backend/app/core/security.py for password hashing. No other folders depend on this folder since these are standalone utilities that don't expose any importable functions or classes.