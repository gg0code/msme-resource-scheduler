AUTO-GENERATED by generate_docs.py on 2026-04-01 18:11
Branch: v4-dev
────────────────────────────────────────────────────────────

FOLDER: backend/tests
BRANCH: v4-dev
PURPOSE: Contains pytest test suite for backend API endpoints, business logic, and the core scheduling engine.

FILES:
  conftest.py - pytest configuration and shared test fixtures for database setup and authentication, v4-dev
  test_availability_engine.py - tests for tenant-scoped resource availability checking logic, v4-dev
  test_employees.py - integration tests for employee CRUD API endpoints, v4-dev
  test_jobs.py - integration tests for job management API endpoints, v4-dev
  test_scheduler.py - unit tests for the pure-Python scheduling engine core algorithms, v4-dev
  test_skills.py - comprehensive integration tests for skills CRUD API endpoints, v4-dev

ARCHITECTURE NOTES:
The conftest.py file provides shared fixtures that other test files depend on for database sessions, test clients, and authentication tokens. Test files are organized by functional area, with some testing pure business logic (like test_scheduler.py for the engine) and others testing full API integration through FastAPI test client. The scheduler tests focus on the pure-Python engine algorithms while other tests verify the complete request-response cycle including database persistence. This separation allows testing both the core scheduling logic in isolation and the full API stack integration.

DESIGN PRINCIPLES:
Principle 1 applies - tests verify that the engine computes while AI only narrates, never allowing LLM to override scheduling decisions. Principle 2 is critical - all database-related tests must verify tenant scoping to prevent security bugs where queries leak data across tenants. Principle 6 applies to scheduler tests which must verify the engine reads from Job/JobStep tables, not legacy SchedJob tables.

GOTCHAS:
All database tests must include tenant_id filtering verification or they will miss critical security bugs that could leak data between customers in production. The scheduler tests are pure unit tests that should never make database calls, while API tests are integration tests that require the full FastAPI application context. Test fixtures in conftest.py create isolated database transactions that rollback after each test, so tests cannot depend on data persisting between test functions.

DEPENDENCIES:
This folder depends on: backend/app/ (all modules being tested), backend/conftest.py fixtures, pytest and testing libraries, database models and schemas for test data creation.
What depends on this folder: Nothing - test files are never imported by application code, they only import and test the application modules.