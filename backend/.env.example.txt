# backend/.env.example
# Copy to .env and fill in values. Never commit .env to git.

# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/msme_scheduler

# JWT — generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=REPLACE_ME

# CORS
ALLOWED_ORIGINS=http://localhost:5173

# These already exist in your .env — keep them
DB_HOST=localhost
DB_PORT=5432
DB_USER=msme_user
DB_PASSWORD=msme_pass
DB_NAME=msme_scheduler
BACKEND_PORT=8000
FRONTEND_PORT=3000

# WhatsApp (v5+) - keep WHATSAPP_MOCK_MODE=True in dev so [MOCK SEND] logs
# appear instead of real Meta sends.
WHATSAPP_MOCK_MODE=True

# Meta Cloud API direct send. Inbound webhook signature verification also
# uses these (always, when not in mock mode). Generate access token via
# Meta dashboard -> System Users.
# WHATSAPP_VERIFY_TOKEN: any opaque string; must match the value entered in
#   the Meta dashboard webhook config.
# WHATSAPP_APP_SECRET:   from Meta App Settings -> Basic. Used for HMAC of
#   inbound webhook bodies (X-Hub-Signature-256).
# WHATSAPP_ACCESS_TOKEN: permanent system-user token (Bearer). 100+ chars.
# WHATSAPP_PHONE_NUMBER_ID: 15-digit numeric ID from Meta -> WhatsApp -> API
#   Setup. Not the phone number itself.
WHATSAPP_VERIFY_TOKEN=
WHATSAPP_APP_SECRET=
WHATSAPP_ACCESS_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=

# v6.3.5 - bot number for the post-signup landing "Open WhatsApp" deep-link.
# Digits only, no leading + (wa.me/<digits> format). Empty = CTA disabled.
WHATSAPP_BOT_NUMBER=
