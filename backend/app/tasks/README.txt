AUTO-GENERATED by generate_docs.py on 2026-04-01 17:59
Branch: v4-dev
────────────────────────────────────────────────────────────

FOLDER: backend/app/tasks
BRANCH: v4-dev
PURPOSE: Contains background task services that automatically manage job lifecycle and step progression.

FILES:
  auto_advance.py - Background loop that automatically advances overdue job steps to keep schedules moving, v4-dev

ARCHITECTURE NOTES:
This folder contains the autonomous task engine that runs independently of user requests to maintain schedule flow. The auto_advance.py service continuously monitors job steps and automatically transitions them when they exceed their planned duration. This folder operates as a background service layer that bridges the scheduler engine with real-time job management. It ensures that schedules don't stall when workers forget to manually advance steps, maintaining system reliability without human intervention.

DESIGN PRINCIPLES:
Principle 1 applies - the auto-advance logic computes step transitions based on time rules, while AI services only narrate what happened. Principle 2 applies - all database queries for job steps and status updates must include tenant_id filtering to maintain data isolation. Principle 6 applies - the auto-advance system reads from Job/JobStep tables directly, not legacy SchedJob tables.

GOTCHAS:
Background tasks run in separate processes from the main FastAPI app, so changes require restarting the task worker, not just the web server. All database operations must use proper tenant scoping since tasks may process multiple tenants in the same execution context. The auto-advance timing logic affects real production schedules, so test thoroughly in development before deploying timing changes.

DEPENDENCIES:
DEPENDS ON: backend/app/models for Job and JobStep ORM models, backend/app/crud for database query functions, backend/app/core/dependencies for database session management, backend/app/services for any notification or logging services.
DEPENDED ON BY: No other folders import from tasks - this folder provides background services that run independently via task queue or cron scheduling.