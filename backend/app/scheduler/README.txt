AUTO-GENERATED by generate_docs.py on 2026-04-01 17:35
Branch: v4-dev
────────────────────────────────────────────────────────────

FOLDER: backend/app/scheduler
BRANCH: both
PURPOSE: Contains the core scheduling algorithm engine that computes optimal job assignments without database dependencies.

FILES:
  engine.py - Pure Python scheduling algorithm that assigns jobs to resources using greedy priority allocation, both branches
  useScheduler.ts - React hook for scheduler toolbar state management and API interactions, both branches
  __init__.py - Package initialization file, both branches

ARCHITECTURE NOTES:
The engine.py file implements a pure Python scheduling algorithm that operates independently of database calls, receiving all necessary data as parameters and returning computed assignments. The useScheduler.ts hook manages the frontend state machine for scheduler interactions and API calls to trigger the backend engine. This folder represents the computational core of the entire scheduling system, with the engine doing all mathematical optimization while the React hook handles user interface state transitions. The scheduler operates on a greedy priority algorithm that considers job priority, resource availability, and tenant-specific constraints.

DESIGN PRINCIPLES:
Principle 1 applies directly - the engine computes optimal schedules while AI services only narrate the results without re-computing logic. Principle 6 ensures the scheduler reads from current Job/JobStep/Machine/Employee tables rather than legacy SchedJob tables. Principle 7 implements resource fallback logic where steps inherit job-level resources when step-specific resources are not defined.

GOTCHAS:
The scheduling engine must receive ALL necessary data as function parameters since it makes zero database calls internally, so missing data will cause silent failures or suboptimal schedules. The engine operates on pure Python data structures and any database session or SQLAlchemy model references will break the zero-dependency design. The useScheduler.ts file appears misplaced in this backend folder and should likely be in frontend/src/hooks/ or similar frontend location.

DEPENDENCIES:
This folder depends on: backend/app/models/ for type definitions of Job/JobStep/Machine/Employee structures passed to the engine, and backend/app/schemas/ for Pydantic response shapes returned from scheduling operations.
This folder is used by: backend/app/routers/scheduler_router.py which loads data from database and calls the engine with parameters, backend/app/services/availability_engine.py for tenant-scoped resource availability calculations, and frontend components that import the useScheduler hook for scheduler toolbar functionality.