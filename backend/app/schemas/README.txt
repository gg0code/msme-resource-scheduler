AUTO-GENERATED by generate_docs.py on 2026-04-01 17:43
Branch: v4-dev
────────────────────────────────────────────────────────────

FOLDER: backend/app/schemas
BRANCH: v4-dev
PURPOSE: Contains all Pydantic v2 request and response validation schemas for API endpoints in the ZetaOps Copilot system.

FILES:
  auth.py - Pydantic schemas for authentication endpoints (login, register, token refresh) - v4-dev
  availability.py - Pydantic schemas for availability override system API requests and responses - v4-dev
  employee.py - Pydantic schemas for employee CRUD operations and API data validation - v4-dev
  job.py - Pydantic schemas for job management, job steps, assignments, and timer operations - v4-dev
  machine.py - Pydantic schemas for machine resource API requests and responses - v4-dev
  resource_availability.py - Pydantic response schemas for job resource availability checking endpoint - v4-dev
  scheduling.py - Pydantic schemas for all scheduling engine requests, responses, and conflict reporting - v4-dev
  skill.py - Pydantic schemas for skills management API endpoints - v4-dev

ARCHITECTURE NOTES:
This folder serves as the API contract layer between the FastAPI routers and the frontend, defining exactly what data shapes are expected for requests and returned in responses. Each schema file corresponds to a major domain entity or system component, with clear separation between input validation schemas and output serialization schemas. The schemas work closely with SQLAlchemy ORM models but are kept separate to allow for different API representations than database structures. All schemas enforce tenant isolation by including tenant_id fields where appropriate. The scheduling.py file is particularly central as it defines the complex data structures used by the core scheduling engine.

DESIGN PRINCIPLES:
Principle 2 applies heavily - tenant scoping is enforced through schema validation, ensuring tenant_id is present on all multi-tenant operations. Principle 8 applies as some schema fields are conditionally validated based on feature flags. Principle 11 is critical since these schemas define the TypeScript types used by the frontend through API contract generation.

GOTCHAS:
Never remove tenant_id from schemas that involve multi-tenant data as this breaks security isolation and could expose data across tenants. Always use Pydantic v2 syntax and Field validators rather than v1 patterns since the entire codebase has been migrated to v2. Be extremely careful when changing existing schema fields as this can break API compatibility with the frontend - always add new optional fields rather than modifying existing required ones.

DEPENDENCIES:
This folder depends on: Pydantic v2 library for base classes and validators, Python datetime and typing modules for type hints, backend/app/models/ for understanding database structure when designing schemas.
This folder is imported by: backend/app/routers/ for request/response validation in all API endpoints, backend/app/services/ for data transformation and business logic operations, frontend type generation tools that convert these schemas to TypeScript interfaces.