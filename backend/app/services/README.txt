AUTO-GENERATED by generate_docs.py on 2026-04-01 17:57
Branch: v4-dev
────────────────────────────────────────────────────────────

FOLDER: backend/app/services
BRANCH: both (core services in v4-dev, WhatsApp services added in v5-whatsapp)
PURPOSE: Contains all business logic services and external integrations for the ZetaOps Copilot system.

FILES:
  ai_service.py - AI Copilot service using Groq LLaMA 3.3-70b for narration and function calling - both branches
  assignment_service.py - Manages job-to-resource assignments and allocation percentages - v4-dev
  auth_service.py - Handles user authentication and tenant registration operations - v4-dev
  availability_engine.py - Core 5-step availability checking algorithm for scheduling conflicts - v4-dev
  cost_service.py - Single source of truth for all cost calculations in the scheduling system - v4-dev
  demo_seeder.py - Creates realistic demo data for new tenants based on industry type - v4-dev
  optimization_engine.py - Additional scheduling optimization utilities - v4-dev
  simulation_service.py - Scheduling simulation and what-if analysis - v4-dev
  token_service.py - JWT token creation and verification for QR code job step execution - v4-dev
  whatsapp_actions.py - Confirmation state machine for write actions in WhatsApp Copilot - v5-whatsapp
  whatsapp_alerts.py - Proactive WhatsApp alert system for job updates and notifications - v5-whatsapp
  whatsapp_bridge.py - Main WhatsApp message handling and routing service - v5-whatsapp
  whatsapp_formatter.py - Converts AI markdown responses to plain text for WhatsApp - v5-whatsapp
  whatsapp_identity.py - Maps WhatsApp phone numbers to tenant accounts - v5-whatsapp
  whatsapp_intent.py - Intent detection for incoming WhatsApp messages - v5-whatsapp
  whatsapp_session.py - Manages conversation history using Redis temporary storage - v5-whatsapp
  whatsapp_whisper.py - Voice note transcription using Groq Whisper large-v3 - v5-whatsapp

ARCHITECTURE NOTES:
The services folder acts as the business logic layer between FastAPI routers and the database/external APIs. Core scheduling services like availability_engine and cost_service provide the computational backbone that the scheduler engine depends on. The ai_service integrates with Groq LLaMA but only for narration, never for recalculating business logic. WhatsApp services form a separate subsystem that bridges external WhatsApp messages to the core scheduling system through identity resolution and intent detection. All services maintain strict tenant scoping and use dependency injection patterns for database sessions.

DESIGN PRINCIPLES:
Principle 1 applies heavily - AI services only narrate results, never recompute scheduling logic that belongs in the engine. Principle 2 is critical as all services must filter database queries by tenant_id for security. Principle 8 applies to WhatsApp services which are feature-flagged. Principle 9 is implemented in WhatsApp services using sync database sessions with async bridge patterns.

GOTCHAS:
All database operations in services must include tenant_id filtering or you create a security vulnerability that leaks data between tenants. WhatsApp services use synchronous database sessions while the rest of the app is async, so bridge functions use run_in_executor for thread safety. The ai_service has function calling capabilities but should never be used to modify data directly - it should only call other services that handle the actual business logic and database writes.

DEPENDENCIES:
This folder depends on: backend/app/models for SQLAlchemy ORM classes, backend/app/schemas for Pydantic validation, backend/app/crud for database query functions, backend/app/core/dependencies for database sessions, backend/app/utils for feature guards and utilities, backend/app/scheduler/engine for core scheduling logic.
This folder is used by: backend/app/routers for business logic in API endpoints, backend/app/tasks for background job processing, other services within this folder that compose functionality from multiple services.