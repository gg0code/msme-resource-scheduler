AUTO-GENERATED by generate_docs.py on 2026-04-01 17:02
Branch: v4-dev
────────────────────────────────────────────────────────────

FOLDER: backend/app/crud
BRANCH: both (used in v4-dev and v5-whatsapp)
PURPOSE: Database query functions that provide CRUD operations for all data models with mandatory tenant scoping.

FILES:
  scheduling.py - Legacy scheduling system CRUD operations for backward compatibility, both branches

ARCHITECTURE NOTES:
This folder contains all database query functions that abstract SQLAlchemy operations from the rest of the application. Each CRUD function is responsible for filtering results by tenant_id to ensure data isolation between tenants. The functions in this folder are called by routers and services to perform database operations without exposing raw SQLAlchemy queries throughout the codebase. This layer sits between the FastAPI routers and the SQLAlchemy models, providing a clean separation of concerns for data access.

DESIGN PRINCIPLES:
Principle 2 applies critically - every single database query must include tenant_id filtering to prevent security bugs and data leaks between tenants. Principle 6 applies as the scheduler reads from Job/JobStep/Machine/Employee models rather than legacy SchedJob tables, though scheduling.py maintains legacy compatibility. The CRUD layer enforces tenant scoping as a security boundary that cannot be bypassed.

GOTCHAS:
Never write a database query without tenant_id filtering or you create a massive security vulnerability that exposes all tenant data. Always use the database session from dependencies.get_db() rather than creating your own session instances. Remember that this is legacy CRUD code for the old scheduling system - new features should use the modern Job/JobStep models instead of the SchedJob tables that scheduling.py accesses.

DEPENDENCIES:
This folder imports from: backend/app/models/ for SQLAlchemy ORM models, backend/app/schemas/ for Pydantic response shapes, sqlalchemy for database session handling.
This folder is imported by: backend/app/routers/ for API endpoints, backend/app/services/ for business logic operations, any code that needs to perform database queries with proper tenant scoping.