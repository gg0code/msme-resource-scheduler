AUTO-GENERATED by generate_docs.py on 2026-04-01 18:02
Branch: v4-dev
────────────────────────────────────────────────────────────

FOLDER: backend/app/utils
BRANCH: v4-dev
PURPOSE: Centralized utility functions for CSV imports, date operations, and feature flag enforcement across the backend.

FILES:
  csv_import.py - CSV parsing and validation utilities for bulk data imports - v4-dev
  date_utils.py - Centralized date utility functions for consistent datetime handling - v4-dev  
  feature_guard.py - Feature flag gating decorators and functions for API endpoints - v4-dev

ARCHITECTURE NOTES:
This folder contains pure utility functions that provide common functionality across the entire backend application. The feature_guard module enforces the feature flag system by providing decorators that can gate API endpoints, while date_utils ensures consistent datetime handling throughout the system. The csv_import module supports bulk data operations for jobs, employees, and machines. These utilities are designed to be stateless and reusable, with no direct database dependencies, making them easy to test and maintain.

DESIGN PRINCIPLES:
Principle 2 applies - tenant scoping must be enforced in any utility that touches tenant data during CSV imports. Principle 8 is core to this folder - feature flags gate all optional features via the FEATURE_FLAGS system implemented in feature_guard.py. Principle 4 applies when these utilities are used in router contexts with /api/ prefixed endpoints.

GOTCHAS:
CSV imports must always validate tenant ownership before processing any records to prevent cross-tenant data leaks. Feature flag checks should be applied at the router level using decorators, not buried deep in business logic where they can be bypassed. Date utilities assume UTC timestamps throughout the system, so any timezone-aware operations need explicit conversion.

DEPENDENCIES:
This folder depends on: backend/app/features_config.py for FEATURE_FLAGS dict, backend/app/core/dependencies.py for tenant context in feature guards, and standard Python libraries for CSV and datetime operations.
What depends on this folder: backend/app/routers/ modules use feature_guard decorators, backend/app/services/ modules use date_utils for scheduling calculations, and various routers use csv_import for bulk operations.