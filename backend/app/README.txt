AUTO-GENERATED by generate_docs.py on 2026-04-01 16:56
Branch: v4-dev
────────────────────────────────────────────────────────────

FOLDER: backend/app
BRANCH: v4-dev
PURPOSE: Contains the core FastAPI application foundation including configuration, database setup, feature flags, and main application entry point.

FILES:
  config.py - Defines all application configuration settings using Pydantic BaseSettings - v4-dev
  database.py - Establishes SQLAlchemy engine and database foundation for the entire application - v4-dev
  features_config.py - Single source of truth for all feature flags in the ZetaOps Copilot application - v4-dev
  main.py - Main FastAPI application entry point with all routers registered - v4-dev

ARCHITECTURE NOTES:
The config.py file provides environment-based settings that are consumed by database.py to establish the PostgreSQL connection and SQLAlchemy engine. The features_config.py defines the FEATURE_FLAGS dictionary that gates optional functionality throughout the application. The main.py file serves as the central orchestrator, importing configuration and registering all API routers under the /api/ prefix. This folder acts as the foundational layer that bootstraps the entire backend application and makes core services available to all other modules.

DESIGN PRINCIPLES:
Principle 3 applies strongly - no .env files are committed to git, all secrets come from settings configuration. Principle 4 is enforced in main.py where all routes get the /api/ prefix. Principle 8 is implemented through features_config.py where FEATURE_FLAGS gate all optional features throughout the system.

GOTCHAS:
Never hardcode configuration values in any file - always use the config.py settings class to maintain environment flexibility. The FEATURE_FLAGS in features_config.py control access to major features so changes here affect the entire application behavior. The main.py file must register ALL routers or endpoints will return 404 errors, and the router registration order can affect middleware behavior.

DEPENDENCIES:
This folder depends on: External packages (FastAPI, SQLAlchemy, Pydantic), environment variables for configuration, and PostgreSQL database connectivity.
What depends on this folder: All other backend folders import from config.py for settings, database.py for DB sessions, features_config.py for feature flags, and main.py serves as the application entry point that everything else plugs into.