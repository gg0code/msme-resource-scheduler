AUTO-GENERATED by generate_docs.py on 2026-04-01 17:01
Branch: v4-dev
────────────────────────────────────────────────────────────

FOLDER: backend/app/core
BRANCH: v4-dev
PURPOSE: Core authentication, authorization, and configuration utilities for the entire FastAPI application.

FILES:
  config.py - Thin compatibility wrapper that re-exports main settings object - v4-dev
  dependencies.py - FastAPI dependency functions for authentication and role-based access control - v4-dev
  plan_limits.py - Plan-based feature and usage limits enforcement for tenant subscriptions - v4-dev
  security.py - JWT token management and password security utilities for authentication - v4-dev

ARCHITECTURE NOTES:
The core folder provides foundational security and configuration services that all other parts of the application depend on. The security.py module handles low-level JWT operations and password hashing, while dependencies.py wraps these into FastAPI dependency injection functions that can be used across all routers. Plan_limits.py enforces tenant subscription boundaries by checking usage against plan allowances. Config.py maintains backward compatibility for settings access patterns used throughout the codebase.

DESIGN PRINCIPLES:
Principle 2 applies critically - tenant scoping is enforced through dependencies.py functions that extract tenant context from JWT tokens and ensure all database queries are properly filtered. Principle 3 is upheld through config.py which manages secrets from settings files rather than environment variables. The authentication design follows principle 1 by keeping JWT verification as pure computation separate from business logic.

GOTCHAS:
Never bypass get_current_user or get_current_tenant dependencies in routers as this breaks tenant isolation and creates security vulnerabilities. JWT tokens must be validated through the security.py functions only - do not attempt custom token parsing as this can introduce authentication bypasses. Plan limits in plan_limits.py are enforced at the API level but not at the database level, so direct database modifications can exceed plan quotas.

DEPENDENCIES:
This folder depends on: app.config for settings, SQLAlchemy models for User/Tenant queries, and external libraries like jose for JWT handling and passlib for password hashing.
This folder is depended on by: All FastAPI routers use dependencies.py for authentication, all services use security.py for token operations, and most tenant-aware operations use plan_limits.py for quota enforcement.